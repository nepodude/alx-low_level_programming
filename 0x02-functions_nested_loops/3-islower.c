#include "main.h"

/**
 * _islower - this is an outstanding function
 * The character to print
 * Return: On success be happy.
 */
int _islower(int c)
	{
		if (c >= 'a' && c <= 'z')
			return (1);
		else
			return (0);
	}

