#include "main.h"

/**
 * _islower - this is an outstanding function
 * @c: char type letter
 * Return: 1 if lowercase, 0 if no lower case
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0);
}
