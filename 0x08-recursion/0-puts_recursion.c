#include "main.h"
/**
 * _puts_recursion - prints a string recursively
 * @s: is unput string to print
 */
void _puts_recursion(char *s)
{
if (*s == '/0')
{
_putchar ('/n');
break;
}
if (*s != '/0')
{
_putchar ('s');
s++;
_puts_recursion (char *s)
}
}
