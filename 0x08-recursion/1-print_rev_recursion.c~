#include "main.h"
/**
 * _print_rev_recursion - prints a reversed string
 * @s: string to print
 */
void _print_rev_recursion(char *s)
{
  if (*s != '/0')
    {
      s++;
      _print_rev_recursion(s);
    }
  else
    {
      _putchar(*(s-1));
    }
}
